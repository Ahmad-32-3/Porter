# IDE chat — paste this

Copy everything below the line into a **new Cursor IDE Agent** chat. Open the workspace on `C:\dev\porter` first (or tell the agent to `move_agent_to_root` / work only in that folder).

---

You are building **Porter** with almost no questions. I will not sit on gates.

**Root:** `C:\dev\porter`. If you are not there, switch to that folder before writing code. Do not edit `C:\dev\grep-vs-embeddings`, `C:\dev\tabular-regimes`, or any personal-site repo.

**Read first:** `docs/HANDOFF.md` in full. Then `.cursor/rules/avoid-ai-writing.mdc` for all prose. Then decide skills.

**ECC skills — you choose them.** They live at `C:\dev\ECC\skills\`. They are not auto-loaded. Scout the catalog (names + SKILL.md descriptions). Pick what this job actually needs: a Go reverse proxy, Redis waiting room, Docker, Vite/React case study, accessibility, security, TDD, unattended MVP. Read those SKILL.md files before you touch that layer. Write the list you picked into `docs/STATUS.md`. Ignore ECC skills for music video, Remotion-as-page, Spring, Vue, Swift, or anything else off-product.

**Autonomy:** Do not ask me which slice to start, whether the palette is fine, or if you should keep going. Plan internally. Build the proxy until tests prove wait/pass/collapse/overflow/privacy. Then Docker Compose + README. Then the case-study site. Fix failures yourself. Use the browser. Update `docs/STATUS.md` as you go.

**Do not git commit or push.** Stop only for paid keys, destructive git, or files outside `C:\dev\porter`.

**Product reminder:** waiting room on `/grades` only; `/tax` stays open; never cache or share private grades; “try anyway” is a capped overflow lane; stale leftover pages are for public paths only.

When `docs/HANDOFF.md` section 12 (Success) is true, stop.
